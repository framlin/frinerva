import {Subject} from "./Subject";
import {Observer} from "./Observer";

    class Observatory {

        offer<T>(subject: Subject<T>) {

        }

        subscribe<T>(observer: Observer<T>) {

        }
    }
export {Observatory}